# cp env.yaml.example env.yaml — then: pnpm deploy:gcp stripe-api
#
#   cp functions/stripe-api/env.yaml.example functions/stripe-api/env.yaml

NODE_ENV: "production"

STRIPE_SECRET_KEY: "FILL_IN"
ALLOWED_ORIGIN: "https://neoncollective.ch"
PUBLIC_SITE_URL: "https://neoncollective.ch"
